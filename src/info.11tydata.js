// const spaces = require(`./_data/${process.env.ELEVENTY_ENV}/spaces`)

// module.exports = {
//     numSpaces: getNumberOfSpaces(),
//     numDistricts: getNumberOfDistricts(),
// }

// function getNumberOfDistricts() {
//     return spaces.map(s => s.spaceData.district).filter((v, i, a) => a.indexOf(v) === i).length
// }

// function getNumberOfSpaces() {
//     return spaces.length
// }